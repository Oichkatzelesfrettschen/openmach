#! /bin/sh
# Generated automatically by configure.
# Run this file to recreate the current configuration.
# This directory was configured as follows,
# on host devbox:
#
# ./configure
#
# Compiler output produced by configure, useful for debugging
# configure, is in ./config.log if it exists.

ac_cs_usage="Usage: ./config.status [--recheck] [--version] [--help]"
for ac_option
do
  case "$ac_option" in
  -recheck | --recheck | --rechec | --reche | --rech | --rec | --re | --r)
    echo "running ${CONFIG_SHELL-/bin/sh} ./configure  --no-create --no-recursion"
    exec ${CONFIG_SHELL-/bin/sh} ./configure  --no-create --no-recursion ;;
  -version | --version | --versio | --versi | --vers | --ver | --ve | --v)
    echo "./config.status generated by autoconf version 2.4"
    exit 0 ;;
  -help | --help | --hel | --he | --h)
    echo "$ac_cs_usage"; exit 0 ;;
  *) echo "$ac_cs_usage"; exit 1 ;;
  esac
done

ac_given_top_objdir="."
ac_given_srcdir="."
ac_given_gsrcdir=".."
ac_given_INSTALL="/usr/bin/install -c"

trap 'rm -fr Makefile
boot/bsd/Makefile
boot/linux/Makefile
bootstrap/Makefile
include/Makefile
kernel/Makefile
kernel/imps/Makefile
libmach/Makefile
libmach/c/Makefile
libmach/exec/Makefile
libmach/standalone/Makefile
libmach/unixoid/Makefile
libthreads/Makefile
mig/Makefile Makeconf conftest*; exit 1' 1 2 15

# Protect against being on the right side of a sed subst in config.status.
sed 's/%@/@@/; s/@%/@@/; s/%g$/@g/; /@g$/s/[\\&%]/\\&/g;
 s/@@/%@/; s/@@/@%/; s/@g$/%g/' > conftest.subs <<\CEOF
/^[ 	]*VPATH[ 	]*=[^:]*$/d

s%@CFLAGS@%-g -O%g
s%@CPPFLAGS@%%g
s%@CXXFLAGS@%%g
s%@DEFS@% -DHAVE_VPRINTF=1 -DHAVE_STRERROR=1 %g
s%@LDFLAGS@%%g
s%@LIBS@%%g
s%@exec_prefix@%${prefix}%g
s%@prefix@%/usr/local%g
s%@program_transform_name@%s,x,x,%g
s%@enable_mp@%%g
s%@enable_libmach@%%g
s%@enable_fipc@%%g
s%@with_elf@%%g
s%@AWK@%mawk%g
s%@LEX@%lex%g
s%@LEXLIB@%%g
s%@YACC@%bison -y%g
s%@INSTALL_PROGRAM@%${INSTALL}%g
s%@INSTALL_DATA@%${INSTALL} -m 644%g
s%@CC@%gcc%g
s%@HOST_CC@%gcc%g
s%@LD@%ld%g
s%@STRIP@%strip%g
s%@AR@%ar%g
s%@RANLIB@%ranlib%g
s%@OBJCOPY@%objcopy%g
s%@mach_subdirs@% boot/bsd boot/linux bootstrap include kernel kernel/imps libmach libmach/c libmach/exec libmach/standalone libmach/unixoid libthreads mig%g

CEOF

CONFIG_FILES=${CONFIG_FILES-"Makefile
boot/bsd/Makefile
boot/linux/Makefile
bootstrap/Makefile
include/Makefile
kernel/Makefile
kernel/imps/Makefile
libmach/Makefile
libmach/c/Makefile
libmach/exec/Makefile
libmach/standalone/Makefile
libmach/unixoid/Makefile
libthreads/Makefile
mig/Makefile Makeconf"}
for ac_file in .. $CONFIG_FILES; do if test "x$ac_file" != x..; then
  # Support "outfile[:infile]", defaulting infile="outfile.in".
  case "$ac_file" in
  *:*) ac_file_in=`echo "$ac_file"|sed 's%.*:%%'`
       ac_file=`echo "$ac_file"|sed 's%:.*%%'` ;;
  *) ac_file_in="${ac_file}.in" ;;
  esac

  # Adjust relative srcdir, etc. for subdirectories.

  # Remove last slash and all that follows it.  Not all systems have dirname.
  ac_dir=`echo $ac_file|sed 's%/[^/][^/]*$%%'`
  if test "$ac_dir" != "$ac_file" && test "$ac_dir" != .; then
    # The file is in a subdirectory.
    test ! -d "$ac_dir" && mkdir "$ac_dir"
    ac_dir_suffix="/`echo $ac_dir|sed 's%^\./%%'`"
    # A "../" for each directory in $ac_dir_suffix.
    ac_dots=`echo $ac_dir_suffix|sed 's%/[^/]*%../%g'`
  else
    ac_dir_suffix= ac_dots=
  fi

  case "$ac_given_top_objdir" in
  .)  if test -z "$ac_dots"; then top_objdir=.
      else top_objdir=`echo $ac_dots|sed 's%/$%%'`; fi ;;
  /*) top_objdir="$ac_given_top_objdir" ;;
  *) # Relative path.
    top_objdir="$ac_dots$ac_given_top_objdir" ;;
  esac
  case "$ac_given_srcdir" in
  .)  if test -z "$ac_dots"; then srcdir=.
      else srcdir=`echo $ac_dots|sed 's%/$%%'`; fi ;;
  /*) srcdir="$ac_given_srcdir" ;;
  *) # Relative path.
    srcdir="$ac_dots$ac_given_srcdir" ;;
  esac
  top_srcdir="$srcdir"
  srcdir="$srcdir$ac_dir_suffix"
  case "$ac_given_gsrcdir" in
  .)  if test -z "$ac_dots"; then gsrcdir=.
      else gsrcdir=`echo $ac_dots|sed 's%/$%%'`; fi ;;
  /*) gsrcdir="$ac_given_gsrcdir" ;;
  *) # Relative path.
    gsrcdir="$ac_dots$ac_given_gsrcdir" ;;
  esac
  top_gsrcdir="$gsrcdir"
  gsrcdir="$gsrcdir$ac_dir_suffix"
  case "$ac_given_INSTALL" in
  .)  if test -z "$ac_dots"; then INSTALL=.
      else INSTALL=`echo $ac_dots|sed 's%/$%%'`; fi ;;
  /*) INSTALL="$ac_given_INSTALL" ;;
  *) # Relative path.
    INSTALL="$ac_dots$ac_given_INSTALL" ;;
  esac

  echo creating "$ac_file"
  rm -f "$ac_file"
  configure_input="Generated automatically from `echo $ac_file_in|sed 's%.*/%%'` by configure."
  case "$ac_file" in
  *Makefile*) ac_comsub="1i\\
# $configure_input" ;;
  *) ac_comsub= ;;
  esac
  sed -e "$ac_comsub
s%@configure_input@%$configure_input%g
s%@top_objdir@%$top_objdir%g
s%@srcdir@%$srcdir%g
s%@top_srcdir@%$top_srcdir%g
s%@gsrcdir@%$gsrcdir%g
s%@top_gsrcdir@%$top_gsrcdir%g
s%@INSTALL@%$INSTALL%g
" -f conftest.subs $ac_given_srcdir/$ac_file_in > $ac_file
fi; done
rm -f conftest.subs



exit 0
